// The Card Master: the NPC (src/sim/content/zone1.ts's `card_master` record,
// `cardMaster: true`) who hosts the Card Duel minigame
// (src/sim/social/card_duel.ts, src/sim/social/card_duel_queue.ts). Any class
// can queue; the NPC is a matchmaking desk, not a vendor.

export const CARD_MASTER_NPC_ID = 'card_master';
