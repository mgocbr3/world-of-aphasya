interface:
  display_name: "World of Aphasya QA"
  short_description: "Coordinate scoped tests and specialist review"
  default_prompt: "Use $woc-qa to validate the current World of Aphasya changes and report whether they are ready."
policy:
  allow_implicit_invocation: true
